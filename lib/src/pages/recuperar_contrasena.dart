import 'package:flutter/material.dart';

class RecuperarContrasena extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: Text('Recuperar contrasena'),
        ),
        body: Center(
          child: Container(
            child: Text('Falta crear vista'),
          ),
      ),
    );
  }
}